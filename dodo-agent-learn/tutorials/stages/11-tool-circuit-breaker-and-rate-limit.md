# 第 11 章：工具熔断与限流

工具执行链按“熔断 → 限流 → 重试 → 超时 → 注册表”固定装配。 // 说明生产链从快速拒绝到真实工具执行的顺序。

- 连续三次最终 `工具执行超时：` 或 `工具执行失败：` 会打开该工具 30 秒。 // 说明熔断触发规则。
- 到期后只有一个半开探测可真实执行；成功恢复，失败重新打开。 // 说明半开并发保护。
- 同一会话调用同一工具最多 10 次/分钟。 // 说明会话与工具维度的滑动窗口。
- 限流、未知工具、参数错误、取消和重复调用跳过不计入熔断失败。 // 说明避免脏状态的边界。
